<h1><%= @driver.name %></h1>
<span><%= @driver.overall_earnings(@driver.id) %> </span>
<span><%= @driver.average_rating(@driver.id) %> </span>


<ul>
  <%@driver.trips.each do |trip|%>
  <li>
    <%=trip.id%>
    <%=trip.date%>
    <%=trip.passenger.name%>
    <%=(trip.cost / 100).round(2) %>
    <%=trip.rating%>
    <%= link_to "Edit Driver", edit_driver_path %>
  </li>
  <%end%>

</ul>

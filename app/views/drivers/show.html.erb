<h1><%= @driver.name %></h1>

<ul>
  <%@driver.trips.each do |trip|%>
  <li>
    <%=trip.id%>
    <%=trip.date%>
    <%=trip.passenger.name%>
    <%=(trip.cost / 100).round(2) %>
    <%=trip.rating%>

  </li>
  <%end%>
</ul>
